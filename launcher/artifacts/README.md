This folder stores launcher build outputs.

- `backend/` contains the packaged Python backend executable used by Electron packaging.
- The editable Python source stays in the workspace root at `../backend/`.
